<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Sharp 90° corners -->
    <corners android:radius="0dp" />

    <!-- Slightly darker gray for stronger contrast with white text -->
    <solid android:color="#BDBDBD" />

    <!-- Subtle bottom border -->
    <stroke
        android:width="1dp"
        android:color="#9E9E9E" />

    <!-- Comfortable spacing -->
    <padding
        android:left="10dp"
        android:top="12dp"
        android:right="10dp"
        android:bottom="12dp" />
</shape>